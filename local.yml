# Generated by `make init` / `make up`. Do not edit by hand — the plugin path
# must be absolute because docker compose resolves relative volume paths
# against the first compose file's directory (moodle-docker's), not ours.
services:
  webserver:
    volumes:
      - __PLUGIN_ABS_PATH__:/var/www/html/local/btcrewards:cached
